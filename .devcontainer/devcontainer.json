{
  "name": "Joomla MCP Suite",
  "dockerComposeFile": "docker-compose.yaml",
  "service": "mcp-server",
  "workspaceFolder": "/workspaces/joomla-mcp-suite",

  "forwardPorts": [8080],
  "postCreateCommand": "chmod +x scripts/auto_install.sh",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
